# 🔧 故障排查指南

## ✅ 已修复的问题

### 1. Next.js 15+ 动态路由参数问题
**问题**：动态路由 `[slug]` 需要使用 async/await 处理 params
**修复**：已更新 `blog/[slug]/page.tsx` 使用正确的异步语法

### 2. 仓库名配置
**问题**：basePath 需要匹配 GitHub 仓库名
**修复**：已更新为 `/personalWebsite`

### 3. Windows Turbopack 符号链接权限问题
**问题**：在 Windows 上本地构建时 Turbopack 需要管理员权限
**解决方案**：
- ✅ GitHub Actions 在 Linux 上运行，不受影响
- ✅ 已添加 cross-env 依赖用于环境变量管理
- 💡 本地开发使用 `npm run dev` 即可，不需要本地构建

---

## 📊 查看部署状态

### 方法 1：通过 Actions 页面查看

1. 访问：https://github.com/stevenchouai/personalWebsite/actions

2. 查看最新的工作流运行状态：
   - 🟡 黄色圆圈 = 正在运行
   - ✅ 绿色对勾 = 部署成功
   - ❌ 红色叉号 = 部署失败

3. 点击工作流可以查看详细日志

### 方法 2：通过提交记录查看

1. 访问仓库主页：https://github.com/stevenchouai/personalWebsite

2. 在最新的提交旁边会显示状态图标

---

## 🚀 启用 GitHub Pages

### 重要提示
如果你还没有启用 GitHub Pages，需要先设置：

1. 进入仓库的 **Settings** → **Pages**

2. 在 **Source** 下选择 **GitHub Actions**

3. 保存后，Actions 会自动运行并部署

---

## 🌐 访问你的网站

部署成功后，网站地址是：
```
https://stevenchouai.github.io/personalWebsite
```

---

## ❓ 常见问题

### Q1: Actions 显示权限错误？

**解决方法**：
1. 进入 **Settings** → **Actions** → **General**
2. 滚动到 **Workflow permissions**
3. 选择 **Read and write permissions**
4. 勾选 **Allow GitHub Actions to create and approve pull requests**
5. 保存

### Q2: 网站显示 404？

**可能原因**：
1. GitHub Pages 还未启用 → 按照上面的步骤启用
2. 部署还在进行中 → 等待 2-5 分钟
3. basePath 配置错误 → 已修复，重新推送代码

### Q3: 样式错乱或资源加载失败？

**检查**：
- `next.config.ts` 中的 `basePath` 和 `assetPrefix` 是否为 `/personalWebsite`
- 已修复，应该没问题

### Q4: 本地无法构建？

**原因**：Windows 上 Turbopack 需要管理员权限

**解决方案**：
1. **推荐**：不需要本地构建，直接推送到 GitHub 让 Actions 构建
2. **本地开发**：使用 `npm run dev` 即可
3. **必须本地构建**：以管理员身份运行 PowerShell/CMD

### Q5: 如何更新网站内容？

**步骤**：
```bash
# 1. 修改代码
# 2. 提交并推送
git add .
git commit -m "更新内容"
git push

# 3. GitHub Actions 会自动构建和部署
# 4. 等待 2-5 分钟后访问网站查看更新
```

---

## 🔍 调试技巧

### 查看构建日志

1. 访问 Actions 页面
2. 点击失败的工作流
3. 点击 **build** 任务
4. 展开各个步骤查看详细日志

### 常见错误信息

#### "Process completed with exit code 1"
- 构建失败，查看详细日志找出具体错误
- 通常是代码语法错误或依赖问题

#### "Error: No uploaded artifact was found"
- build 任务失败，导致没有产物可部署
- 修复 build 错误后重新运行

#### "Error: Resource not accessible by integration"
- Actions 权限不足
- 按照 Q1 的方法设置权限

---

## 📝 下一步

1. ✅ 确认 GitHub Pages 已启用
2. ✅ 等待 Actions 构建完成
3. ✅ 访问网站：https://stevenchouai.github.io/personalWebsite
4. 🎨 开始自定义你的网站内容！

---

## 📚 相关文档

- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Next.js 静态导出](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)

---

**需要更多帮助？** 查看 Actions 日志或提 Issue！


